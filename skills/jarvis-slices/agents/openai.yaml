interface:
  display_name: "Jarvis Slices"
  short_description: "Plan and execute slice-first work with checkpoints"
  default_prompt: "Use $jarvis-slices to split this work into approved slices with checkpoints."
