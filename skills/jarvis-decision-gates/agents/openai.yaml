interface:
  display_name: "Jarvis Decision Gates"
  short_description: "Options, recommendations, and approval gates"
  default_prompt: "Use $jarvis-decision-gates before choosing the next major step."
