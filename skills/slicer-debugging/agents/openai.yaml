interface:
  display_name: "Slicer Debugging"
  short_description: "Root-cause debugging gate before any fix"
  default_prompt: "Use $slicer-debugging to investigate this failure and confirm root cause before editing."
