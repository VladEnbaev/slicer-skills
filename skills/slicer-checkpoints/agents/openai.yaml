interface:
  display_name: "Slicer Checkpoints"
  short_description: "Continue checkpoints between verified slices"
  default_prompt: "Use $slicer-checkpoints to choose what happens after this verified slice."
