interface:
  display_name: "Slicer Planning"
  short_description: "Plan readable slice maps with boundaries and verification"
  default_prompt: "Use $slicer-planning to split this work into a readable slice map."
